# libsignal backend (not linked)

This directory is a **placeholder** so the harness architecture is complete
without importing AGPL code.

- `PIN.md` — revision VoiceChat uses (currently unknown).
- No Rust sources here compile into `crypto-parity`.
- The stub lives in `src/libsignal_ref.rs` and always returns `NOT_LINKED`.

Third-party use of libsignal is unsupported by Signal; treat any linked
reference as a private test appliance only.
